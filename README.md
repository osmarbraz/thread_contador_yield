# Contador com Java Thread e Yield.

- Permite executar várias instâncias da classe Contadora em threads e usar yield para liberar a CPU e dar a vez para outras threads.
- Cria a Thread a interface Runnable.